// This might work as an interface. Not sure if the methods would need to be shared between other classes

public class DML {

    // Instance variables go here

//    INSERT INTO lab7_rooms SELECT * FROM INN.rooms;
//
//    // DATE_ADD shifts reservation dates to current year
//    INSERT INTO lab7_reservations SELECT CODE, Room,
//    DATE_ADD(CheckIn, INTERVAL 134 MONTH),
//    DATE_ADD(Checkout, INTERVAL 134 MONTH),
//    Rate, LastName, FirstName, Adults, Kids FROM INN.reservations;

    // DML Constructor

    // methods: Insert into rooms, Insert into reservations

}


